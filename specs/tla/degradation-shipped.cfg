\* THE BEHAVIOUR THAT SHIPPED. Both faults live: `||` substitutes on
\* revocation, and the run greens on whatever it reached. This is
\* content-catalog's four workflows on 2026-08-16.
\*
\* Both invariants break here, so which one TLC reports first is an artifact
\* of its search order — check.sh therefore asserts only that a violation is
\* found in this config, and leaves the per-fault assertions to the two
\* isolating configs below, where exactly one invariant can break.
SPECIFICATION Spec
CONSTANTS
  Repos          = {r1, r2}
  FallbackCovers = {r1}
  AllowFallback  = TRUE
  GreenOnPartial = TRUE
INVARIANT TypeOK
INVARIANT NoUndernamedEffect
INVARIANT NoSilentPartial
