\* Racy mechanism, anonymous fallback live — the behaviour shipped today.
\* Expect TLC to report a counterexample: the upload trusts the preflight
\* read, so consent withdrawn during the derive window is not seen.
SPECIFICATION Spec
CONSTANTS
  Sessions          = {s1, s2}
  Atomic            = FALSE
  AllowAnonFallback = TRUE
INVARIANT TypeOK
INVARIANT NoUnconsentedDisclosure
INVARIANT NoAnonUpload
