\* Safe mechanism — expect TLC to PASS every invariant and the liveness
\* property. Revocation refuses instead of substituting, and success is
\* reported only over a run that reached every target.
SPECIFICATION Spec
CONSTANTS
  Repos          = {r1, r2}
  FallbackCovers = {r1}
  AllowFallback  = FALSE
  GreenOnPartial = FALSE
INVARIANT TypeOK
INVARIANT NoUndernamedEffect
INVARIANT NoSilentPartial
PROPERTY Termination
